# TextSpeedReader

TextSpeedReader 是一款基於 .NET Windows Forms 開發的多功能文字閱讀與編輯器，旨在提供流暢的檔案瀏覽與多樣化的文字處理工具。

## 專案用途

本工具主要用於快速開啟、閱讀並編輯各類文字檔案。除了基本的編輯功能外，它還整合了檔案管理、編碼修正以及繁簡轉換等進階功能，特別適合開發者、文字工作者或需要處理不同編碼檔案的使用者。

## 主要功能

### 檔案瀏覽與管理
- **樹狀目錄 + 檔案清單**：TreeView 顯示目錄結構（惰性載入），ListView 顯示當前目錄內的檔案。
- **多格式支援**：可在編輯器中開啟 `.txt`, `.cs`, `.yaml`, `.js`, `.py`, `.md`, `.css`, `.json`；以 WebBrowser 預覽 `.html`, `.htm`。
- **拖曳開啟**：支援從 Windows 檔案總管拖曳檔案或資料夾至程式視窗。
- **檔案拖曳操作**：
  - 拖曳 ListView 中的檔案至 TreeView 目錄：按住 **Ctrl** 為複製，放開為搬移
  - 支援多選項目同時拖曳
  - 搬移後自動從 ListView 移除已搬走項目
- **資料夾操作**：新增子目錄、刪除（移至資源回收筒）、重新命名，亂碼修正，繁簡轉換。
- **檔案操作**：刪除（移至資源回收筒）、重新命名，單檔與多檔批次更名皆支援，繁簡轉換。
- **檔名搜尋**：在 ListView 中以關鍵字搜尋並選取符合的檔案。
- **在多個檔案中搜尋文字** (Ctrl+Shift+F)：對目前 ListView 顯示的所有文字檔案進行全文搜尋，以 TreeView 階層顯示「檔案 → 命中行」，點擊結果即開啟該檔案並反白比對字。支援半/全形視為相同、大小寫切換；命中字以黃底紅字粗體凸顯；大檔案（>5MB）與大量命中（每檔 50 筆 / +100 批次）會彈窗確認，避免效能卡頓。

### 文字編輯
- **RichTextBox 編輯器**：支援字型選擇、字體大小調整（預設尺寸清單）、自動換行切換。
- **尋找 / 取代**：
  - 支援「區分大小寫」與「全字匹配」
  - 「尋找下一個」與「尋找上一個」雙向搜尋，均支援循環查找
- **拖曳選取文字**：可拖曳外部文字直接插入。

### 文字處理工具
- **移除多餘斷行**：合併段落內的單行換行，保留雙換行（段落分隔）。
  - 進階版：可保留以句號、問號、驚嘆號結尾的行換行。
- **自動移除多餘的斷行**：自動移除多餘的斷行，跳過行尾句點或驚嘆號。每行行尾的半形空格、TAB、全形空白（U+3000）會在判定前自動剝除，避免被尾端空白影響判定。
- **半形標點符號轉全形**：將 `, . ! ? : ; ( )` 等一般文章標點批次轉成對應的中文全形標點；可作用於選取範圍或全文。
- **確保段落空白行**：讓每對非空行之間恰好有一個空白行。
- **移除空白行**：移除所有空白行，使各行緊密排列。
- **移除行首/尾空白**：清除半形空格、TAB、全形空格。
- **行首加縮排**：為每個非空行加上指定數量的空白字元；空白數量與「半形/全形空白」皆可在「選項」視窗中調整（預設全形空白 U+3000）。
- **行尾補句號**：對未以標點結尾的行自動補上全形句號「。」。
- **行排序**：正向 / 反向排序，支援保留空白行的分段排序模式。
- **依判定字串拆行 / 合行**：可在設定中指定「新行開頭判定字串」與「新行結尾判定字串」，對文字進行段落切分或合併。
- **插入行首/尾標記**：為每個非空行加上設定中指定的開頭與結尾字串。
- **行號標記插入**：
  - 在選取範圍的每行行首插入「AnnotationBegin + 單位序號 + AnnotationEnd」
  - 編號為四位數零填補格式（例：`/*0001*/`）
  - 支援大括號 `{...}` 區塊：`{` 必須行首、`}` 必須行尾，支援巢狀，整個區塊視為一個單位，只佔一個編號
  - 已有標記時自動更新序號
- **超過指定字數自動分行**：彈窗詢問每行字數上限（預設 120），對超出長度的行於下一個句號/驚嘆號/問號處自動斷行。

### 編碼修正
- **智慧編碼修正（ReCodeText）**：將目前文字以「錯誤編碼 → 正確編碼」重新解碼，預覽多種組合後選取。支援可選同步執行簡→繁轉換。
- **檔名編碼修正（ReCodeFileName）**：修正 ListView 中選取的檔案或目錄名稱的編碼錯誤。
- **目錄名稱編碼修正（ReCodeFolderName）**：修正 TreeView 中選取目錄的名稱編碼。
- **批量編碼修正（ReCodeFullFoldersFilesName）**：遞迴修正指定目錄下所有檔案與子目錄的名稱編碼。
- 支援的編碼組合：UTF-8、UTF-8-BOM、GBK（936）、Big5（950）、GB18030（54936）、Unicode（UTF-16 LE）。

### 繁簡轉換
- **文字內容轉換**：將目前開啟的 `.txt` / `.json` 轉換為簡體並另存（檔名加 `_簡體1`）。
- **批次轉繁/轉簡**：對 ListView 中選取的多個 `.txt` / `.json` 批次轉換並另存。
- **檔名繁簡轉換**：將 ListView 中選取的檔案或目錄名稱以 OpenCCNET 轉為繁體。
- **目錄名稱繁簡轉換**：將 TreeView 中選取目錄的名稱轉為繁體。
- 使用 OpenCCNET 函式庫（`HantToHans` / `HansToTW`）。

### 儲存功能
- **覆蓋儲存**：以原始編碼覆蓋目前檔案。
- **另存新檔**：以 UTF-8-BOM 儲存，支援多種副檔名。
- **選取內容另存**：將目前選取的文字另存為新檔，自動產生不重複的三位數序號檔名。
- **整篇分割儲存**：將整篇文字以每份 3,000 字切割，批次存為多個序號檔案。

### HTML 瀏覽（WebBrowser 模式）
- 自動偵測 HTML 檔案編碼（GB2312/GBK → UTF-8），必要時產生臨時檔案確保正確顯示。
- 可調整頁面縮放比例（預設縮放清單）。
- 複製 WebBrowser 選取文字並轉換為繁體/簡體後儲存為 `.txt`。

### 閱讀紀錄
- 自動儲存最近閱讀的檔案路徑與閱讀字元位置，方便續讀。
- 支援「自動開啟上次目錄」。
- 最近目錄與最近檔案各保留最多 10 筆（MRU 順序）。

## 技術架構

| 項目 | 內容 |
|------|------|
| 框架 | .NET Windows Forms |
| 語言 | C# |
| 主要表單 | `FormTextSpeedReader`（分拆為多個 partial class 檔案） |
| 設定儲存 | INI 格式，`.\TextSpeedReader_Settings.ini` |
| 編碼偵測 | UDE（Universal Charset Detector）+ BOM 判斷 |
| 繁簡轉換 | OpenCCNET |
| 檔案刪除 | `Microsoft.VisualBasic.FileIO`（使用資源回收筒） |

## 更新紀錄

### V2.12.0.0 - 2026-06-15
- **新增 Ctrl+Shift+F「在多個檔案中搜尋文字」**：
  - 模仿 IDE 的多檔案搜尋；對目前 ListView 顯示的所有文字檔做全文搜尋
  - 結果以 TreeView 階層呈現：父節點＝檔案（含命中數），子節點＝「行 NNN: 比對字附近片段」（截斷顯示）
  - 子節點單擊即在主視窗開啟該檔案、捲動到該行，並以「被選取」狀態（藍底白字）反白比對字本身
  - 比對字在結果列表中以**黃底紅字粗體**凸顯（OwnerDraw 實作）
  - 「半形/全形視為相同」選項：將全形 ASCII（U+FF01~U+FF5E）與全形空白（U+3000）正規化為半形後比對；「大小寫相符」選項可獨立切換
  - 彈窗永遠顯示於主視窗之上（TopMost），靠齊螢幕左側，寬 600、高為螢幕 80%
  - 效能保護：
    - 單檔 > 5 MB 時跳出 4 選 1 對話框（跳過所有/跳過此檔/讀取所有/讀取）
    - 單檔命中達 50 筆時跳出對話框（繼續搜尋下 100 筆/跳過後續搜尋）
  - 編碼採用 `JTextFileLib.DetectEncoding`（UDE 函式庫），支援 Big5 / GBK / UTF-8 / UTF-16，繁中檔不會出現亂碼
- **行號標記插入 `{...}` 區塊規則改寫**：
  - `{` 必須位於行首才視為區塊開頭
  - `}` 必須位於行尾才視為區塊結尾（不限定行首）
  - 支援巢狀大括號，最外層整個區塊視為單一單位、只佔一個編號
  - 修正 `{/*xxx` 已有 startMark 但無數字時，編號插入位置錯誤的 bug
- **AutoRemoveCRWithoutDotAndExclamationMark 強化**：每行尾的半形/全形空白與 TAB 在判定結尾標點前先剝除，避免因尾端空白導致判定失效。
- **「將半形標點符號改成全形」右鍵選單**：批次將 `, . ! ? : ; ( )` 等標點轉為對應全形。
- **選項視窗新增「全形空白字元」勾選框**：搭配「行首加縮排」功能，可選擇使用半形空格或全形空白 U+3000，並寫入 INI（`AddSpaceUseFullWidth`）。
- **超過指定字數自動分行**：原本固定 120 字硬編碼，改為先彈窗詢問使用者字數上限（預設 120，下限 10）。
- **修正：含逗號的目錄無法用「開啟檔案總管」開啟**：`explorer.exe` 會把逗號當引數分隔符，現在會將完整路徑以雙引號包起後再傳入。
- **修正：搜尋結果視窗的字體與行高、按鈕對齊問題**：統一改為 12pt JhengHei UI，TreeView 行高隨字體自動計算；「搜尋」按鈕高度加高與輸入框視覺對齊。

### V2.11.0.0 - 2026-06-11
- **行號標記插入優化**：
  - 編號格式改為四位數零填補（例：`0001`, `0002`）
  - 新增 JSON 風格 `{...}` 區塊支援：
    - 以 `{` 為行首表示多行複合體，整個區塊視為一個單位，不管中間有多少行
    - 若 `{` 後接 `/*`，在 `/*` 後插入編號；否則在 `{` 後插入 `/*編號*/`
    - 區塊內的行原樣保留，不額外編號，只消耗一個單位序號
- **尋找功能增強**：
  - 新增「尋找上一個(&P)」按鈕，支援反向搜尋
  - 反向搜尋亦支援「區分大小寫」與「全字匹配」選項
- **程式關閉保護**：
  - 關閉程式時，若目前編輯的檔案有未儲存的更動，先彈出「確認儲存」對話框
  - 避免誤關卻沒有儲存編輯內容的情況

### V2.10.0.0 - 2026-05-20
- **新增「文字處理工具」**：
  - 移除上方選單"整理編排段落"(功能重複)
  - 新增"自動移除多餘的斷行，跳過行尾句點或驚嘆號。"

### V2.9.0.0 - 2026-05-18
- **檔案拖曳搬移/複製功能**：
  - 支援從 ListView 拖曳檔案至 TreeView 目錄
  - 按住 **Ctrl** 為複製，放開為搬移
  - 支援多選項目同時操作
  - 搬移後自動從 ListView 移除已搬走的項目
- **檔案與資料夾操作功能強化**：
  - 目錄區新增"新增子目錄..."功能

### V2.8.0.0 - 2026-05-12
- **Claude Code優化程式碼，大量註解，添加SKILL.md，大幅添加README.md內容**：
  - 以Claude Code優化程式碼
  - 大量繁體中文註解，使程式碼易懂
  - 添加SKILL.md、SPEC.md、CLAUDE_andrej-karpathy-skills.md，記錄程式開發規範
  - 大幅添加README.md內容，說明本程式功能與使用方式

### V2.7.1.0 - 2026-05-12
- **JSON 支援**：
  - 支援 `.json` 檔案格式。
  - 加入 JSON 檔案的編碼修正功能。
  - 加入 JSON 檔案的另存新檔功能。

### V2.7.0.0 - 2026-05-09
- **強化編碼修正功能 (ReCodeText)**：
  - 實作針對目前編輯中文字的編碼修正功能。
  - 支援選取部分文字作為預覽樣本，以便精確判斷原始編碼。
  - 加入「自選編碼重新命名」視窗，提供直觀的編碼轉換預覽。
- **擴展 UTF-8-BOM 支援**：
  - 在所有編碼轉換與預覽邏輯中加入 `UTF-8-BOM` 格式。
  - 確保檔案儲存時能正確寫入 UTF-8 識別標籤。
- **支援更多程式碼與文件格式**：
  - 將 `.js`, `.py`, `.md`, `.css` 加入支援列表。
  - 更新儲存對話框的過濾器（Filter），讓使用者能以正確的副檔名儲存檔案。
- **優化檔案類型管理**：
  - 重構 `FileSystemManager` 與 `FormTextSpeedReader` 之間的連動，使新增支援格式更加容易且統一。

### V2.6.1.0
- 修正編輯框無法塞滿視窗問題。

### V2.6.0.0
- 自動換行功能。
- 多個 AI 生圖提示詞方便編輯功能（插入行號標記、依判定字串拆行/合行等）。

---
*本專案由 Antigravity 輔助開發與優化。*
